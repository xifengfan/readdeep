name = "readdeep"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

[build]
# Cloudflare Pages 直接把仓库根目录作为静态文件
# functions/ 目录会自动被识别为 Pages Functions
# /api/* 路由需放在 functions/api/ 子目录

[vars]
# 公共环境变量（非敏感）
AGENTS_VERSION = "v2.2"
P2_PHASE = "D2-0-1-dogfooding"
